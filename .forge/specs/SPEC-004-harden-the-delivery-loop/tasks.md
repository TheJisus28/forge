# Tasks — SPEC-004

- [x] Phase 1 — `forge guard --command` plus the Bash hooks (Claude and
  OpenCode). Where: `internal/cli/guard.go`, `internal/cli/init.go`,
  `kit/opencode/plugins/forge-guard.js`.
- [x] Phase 2 — `## Open questions` gate on approve and validate. Where:
  `internal/project/project.go`, `internal/cli/work.go`,
  `internal/validate/validate.go`, `kit/forge/kit/templates/spec.md`.
- [x] Phase 3 — task progress in brief and status detail. Where:
  `internal/project/project.go`, `internal/view/view.go`.
- [x] Phase 4 — remove `forge board` and `BOARD.md`. Where:
  `internal/cli/report.go`, `internal/cli/cli.go`, `internal/view/view.go`,
  `internal/cli/init.go`, docs.

## Proposed conventions

None.
