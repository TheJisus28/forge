# Tasks — SPEC-002

- [x] Phase 1 — `GHUser`, `Push`, and `resolveActor` prefers the gh login.
  Where: `internal/project/git.go`, `internal/cli/work.go`.
- [x] Phase 2 — `forge submit` pushes, opens the PR, records it, never merges.
  Where: `internal/cli/report.go`, `internal/cli/cli.go`.
- [x] Phase 3 — kit and docs state the pull request as the end of the loop.
  Where: `kit/`, `docs/`.

## Proposed conventions

None.
