# SPEC-XXX — title

Contract. The implementer builds this; QA verifies this.

The design must be valid for `forge/memory/stack.md`. Do not assume
another language.

You may write this file in the `working_language` from
`forge/memory/constitution.md`. Keep IDs, status values, and error
codes in English.

## 1. Intent

One sentence: what changes.

## 2. Out of scope

Closed list.

## 3. Decisions

Numbered. Chosen option, discarded alternatives, why.
If it must outlive this SPEC, mark `→ memory/decisions` or `→ ADR`.

## 4. Design

Modules, routes, schema, screens. Real names from **this** repo.

## 5. Acceptance criteria

- [ ] AC1: observable (test, command, screenshot)
- [ ] AC2: …

## 6. Non-regression

What must not break (HTTP contract, auth, migrations, …).
