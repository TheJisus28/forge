# Review — SPEC-000

Verdict: pass | fail | pass with notes

## Acceptance criteria

One line per criterion, naming it (`ACn`): pass or fail, plus the evidence
that settles it — the command and its output, the test name, the request and
the response. Evidence belongs under this heading; `forge check` reads only
this section, so a mention under `## Notes` does not count.

| Criterion | Result | Evidence |
|---|---|---|

<!-- Example rows, not evidence: a reviewer who copies them without editing
     would look covered, so the shipped template carries no real criterion id.
| ACn | pass | command and output, or test name |
| ACn | fail | what happened instead |
-->

## Blocking problems

What must be fixed before merging.

## Notes

What can be merged as is.

## Proposed conventions

None.
